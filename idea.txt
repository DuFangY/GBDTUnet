由于GDBT四分类 所以每个默认几十个分类器 其中每个分类器都有4棵树去决策属于每一个类的概率


找到每一个类(预测的结果)的决策规则 去除重复 找到路径 用找到的路径看是否与unet分割相似  可能会存在多条类判别路径，后续找方法判断是否正确


GDBT 比 RF 好，Xgboost比GDBT好


GDBT和Xgboost都是利用残差训练模型

也可用作rulefit

########################################################

GDBT 二分类  才可以提取规则 (或可用作rulefit）